[project]
name = "scribe-api"
version = "0.1.0"
description = "ekascribe API (voice2rx-be fork)"
requires-python = ">=3.11"
dependencies = [
    "scribe-core",
    "echo[sarvam]",
    # web
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "orjson>=3.10",
    "pydantic>=2.7",
    "PyJWT>=2.10",
    "python-dateutil>=2.9",
    "requests>=2.32",
    "httpx>=0.27",
    "PyYAML>=6.0",
    "jsonpatch>=1.33",
    "ag-ui-protocol>=0.1.0",
    "mcp",
    # aws (s3/dynamo backends; endpoint-overridable)
    "aioboto3>=15.0.0",
    "boto3>=1.38",
    # db / state
    "asyncpg>=0.30",
    "redis[hiredis]>=5.0",
    # audio
    "pydub>=0.25",
    # Python 3.13 removed stdlib audioop (pydub needs it); this restores it
    "audioop-lts>=0.2; python_version >= '3.13'",
    # llm providers reachable through echo
    "openai>=1.0",
    "anthropic>=0.18",
    "google-genai>=1.0",
    "langfuse>=2.0",
    # documents / publish (feature-flagged, code retained)
    "markdown>=3.6",
    "weasyprint>=62.3",
    "python-docx>=1.1.2",
    "striprtf>=0.0.26",
    "google-api-python-client>=2.160",
    "google-auth>=2.38",
    # fhir (feature-flagged, code retained)
    "scribe2fhir",
]

[tool.uv.sources]
scribe2fhir = { git = "https://github.com/MedScribeAlliance/scribe2fhir.git", subdirectory = "python" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/scribe_api", "src/voice2rx", "src/medical_record", "src/logs"]
