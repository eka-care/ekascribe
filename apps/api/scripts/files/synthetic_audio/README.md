Synthetic (non-speech) audio fixtures — replace the real consultation
recordings that were excluded from the public repo (plan decision #13).
Tones exercise the upload → chunk → transcribe plumbing; STT will return
empty/near-empty text. For a speech fixture, record a scripted fake
consultation (see DEMO_SCRIPT_TEXT in apps/web/src/constants/constant.ts)
and drop the m4a files here.
