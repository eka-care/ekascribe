FROM node:22-alpine
WORKDIR /app

# RUN npm install -g yarn@latest

COPY . .

RUN yarn install

RUN cd packages/ui-lib && yarn install

RUN yarn build || echo "TRUE"

EXPOSE 80

CMD ["yarn", "dev:local"]
